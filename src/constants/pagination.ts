// Import pagination constants from backend
import { PAGINATION_CONFIG as BackendPaginationConfig } from '../../../Backend/src/shared/constants/pagination';

// Re-export the backend's pagination config
export const PAGINATION_CONFIG = BackendPaginationConfig;